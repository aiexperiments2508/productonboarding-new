# Optional: LangGraph Studio.
#
#   pip install -r requirements-studio.txt
#   langgraph dev
#
# Then open the Studio UI it prints:
#   https://smith.langchain.com/studio/?baseUrl=http://127.0.0.1:2024
# It needs a LangSmith sign-in (free tier); the graph itself runs locally.
#
# Studio is a development view, not a dependency. The Approvals & Audit tab
# shows the same run trace without it, so a demo never relies on an external
# service being reachable.

langgraph-cli[inmem]>=0.4,<1
