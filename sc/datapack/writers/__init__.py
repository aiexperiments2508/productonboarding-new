"""The five output formats.

``csv_txt`` and ``jsonschema`` are standard library. ``specdoc`` is standard
library too - a .docx is a zip of XML and this one is prose and tables.
``workbook`` is the only module here that needs anything installed, and it says
so rather than failing at import.
"""
